# 《沧海余生》后续开发计划

| 字段 | 内容 |
| --- | --- |
| 文档版本 | v0.3 |
| 更新日期 | 2026-08-06 |
| 当前阶段 | R0–S6 已落地；S7 MVP 整合进行中（S7-01～S7-04 已完成，S7-05 平衡进行中） |
| 目标引擎 | Godot 4.7.x / Compatibility Renderer |
| 依据 | `01-游戏立项方案.md`、`02-核心玩法原型方案.md`、`03-生存系统策划案.md`、`系统关系架构文档.md`、当前工程与架构规范 |
| 本次核对范围 | Source、Scenes、Data、Tests、最近提交记录；已使用本机 Godot 4.7.1 运行 `verify_project.ps1 -Full`，96 项单元测试通过 |

---

## 1. 计划目标与实施原则

本计划将项目推进至可验证的单机 2D 核心玩法 MVP。MVP 的玩家循环保持为：

```text
探索航行 → 遭遇 / 资源 / 伙伴 → 收集与加工 → 建造与扩建
    → 生存维持 → 编队与战斗 → 奖励和成长 → 下一次探索
```

当前实现遵循以下优先级：

1. 同一系统发生冲突时，以最新、最细的策划案为玩法口径；
2. 所有程序实现必须遵守 `Architecture.md`、`DataDesign.md` 与 `CommandAndEvents.md`；
3. `系统关系架构文档.md` 中的高层系统图用于说明玩法依赖，不能覆盖当前工程的 `GameSession → System → State` 边界；
4. 先完成一条可保存、可测试、可从正常 UI 推进的链路，再扩充内容数量；
5. 不新增玩法 Autoload、万能 Manager、联网、账号、云存档或第三方玩法框架。

每项功能的完成条件不只是“存在脚本或场景”，而是同时具备：真实玩家入口、明确的 State 修改权、Definition 配置、存档策略、自动测试及桌面/触摸主流程验证。

---

## 2. 当前开发基线（2026-08-06）

### 2.1 已经落地的能力

| 领域 | 状态 | 当前实现与证据 |
| --- | --- | --- |
| S0：工程与验证基线 | 已落地 | `Tools/verify_project.ps1`、零依赖 `Tools/run_tests.gd`、数据/场景/运行时验证脚本；SaveService 已到 v4 并具备 v1→v4 迁移链。 |
| 会话与规则边界 | 已落地 | `GameSession` 持有 Inventory、Building、Gathering、Survival、Production 与 SimulationClock；`GameState` 聚合可存档状态。 |
| S1：建造 | 核心闭环已完成 | 初始 3×3 竹筏、库存扣费、5 个 1×1 设施、等距格选择/预览/确认/取消、鼠标与触摸输入、建筑实例及占格存档。 |
| S2：生存 | 已收口 | 补给/耐久/体力、5 秒结算、阈值状态、被动恢复、HUD、食物恢复、存档与纯规则测试均已存在。 |
| S3：采集、生产与恢复 | 首条闭环已完成 | 手动采集木材/生鱼/海水；集雨器产淡水、篝火产烤鱼、维修站恢复耐久、储水罐扩容；设施启停、缺料/满仓停摆、生产进度和存档已实现。 |
| R0：生存与指令收口 | 已落地 | `SurvivalSystem` 提供体力预检/原子扣除/耐久损耗公开接口；离线结算幂等可重入并生成只读报告；Gather、UseFood、SetProductionEnabled 统一经 Command 派发。 |
| S4：大地图与确定性探索 | 已落地 | 4 个已规划海域 + 动态远洋、船舵解锁、同 Seed 同遭遇、资源/空事件/风暴/救援四种结果、地图 View 与世界存档往返。 |
| S5：伙伴卡牌与编队 | 已落地 | 4 名伙伴、招募（唯一性）、Lv1–Lv10 升级、2 个出战位、伙伴面板与存档往返。 |
| S6：教学 Boss 战 | 主体已落地 | `BattleSystem` 纯规则、教学 Boss、普通攻击/技能/冷却、胜负与掉落/耐久结算、`BattleRoot` 场景流程、存档往返测试。 |
| S7-01：竹筏成长与解锁 | 已落地（本次） | `ProgressionSystem` 接管竹筏 Lv1→Lv3、3×3→5×6 扩建与数据驱动船舵解锁；`ProgressionState` 序列化；存档 v8→v9 迁移；设施面板新增竹筏扩建入口。 |
| S7-02：设施扩充与区域 Boss | 已落地（本次） | 新增海水淡化器、渔网、储物架（设施 5→8）；新增蒸馏海水与收网捕鱼配方；新增暗礁巨鳗区域 Boss（竹筏 Lv2 解锁）与专属奖励，Boss 解锁经 `ProgressionSystem` 数据规则校验。 |
| S7-03：流浪商人模板 | 已落地（本次） | `MerchantDefinition`/`MerchantOfferDefinition` 数据模板、`MerchantState` 库存存档（v9→v10 迁移）、`MerchantSystem` 原子交易、`BuyMerchantItemCommand` 统一派发与商人面板/导航入口。 |
| S7-04：新手目标与导航 | 已落地（本次） | `QuestDefinition`/`QuestSystem` 数据驱动目标（建造/探索/招募/扩建/Boss）、`QuestState` 完成标记与击败 Boss 持久化（v10→v11 迁移）、目标面板与“目标”导航按钮、一键跳转到对应面板。 |
| 数据与测试 | 已落地 | 已有 Item、Building、Recipe、Survival Definition；覆盖建造、生产、存档、生存计算、网格、随机与数据注册的单元测试。 |
| UI 与场景流 | 可玩原型 | MainMenu 可创建 Session 并进入 GameRoot；已接入建造视图、建造面板、生存 HUD 与生产面板。 |

当前首条可玩链为：

```text
采集木材 / 生鱼 / 海水
  → 建造集雨器、篝火、维修站或储水罐
  → 生产淡水 / 烤鱼，或恢复耐久、扩充淡水容量
  → 手动食用淡水 / 烤鱼恢复补给
  → 继续采集与扩建
```

现有内容包括：`item_wood`、`item_raw_fish`、`item_seawater`、`item_fresh_water`、`item_grilled_fish`；8 个设施（集雨器、篝火、维修站、储水罐、船舵、海水淡化器、渔网、储物架）；四条生产配方、四个海域、四名伙伴、教学 + 区域 Boss、一名流浪商人（3 个交易条目）、五条新手目标链和 `survival_default` 配置。

### 2.2 不应误判为“已完成”的缺口

| 范围 | 已有基础 | 进入下一阶段前需要补齐 |
| --- | --- | --- |
| 设施与 Boss 数量 | 8 个设施、教学 Boss + 区域 Boss | 已达 MVP 数量目标，按试玩结果微调。 |
| 流浪商人 | 1 个商人模板、3 个交易条目 | 已落地；后续按试玩结果扩充货物与刷新节奏。 |
| 新手目标与导航 | 5 个数据驱动目标 + 目标面板一键导航 | 已落地；返回路径已覆盖建造/补给/地图/伙伴/商人/战斗/离线。 |
| 平衡与试玩 | 首条链与探索/战斗均可操作 | S7-05 完成 20–30 分钟试玩并记录调参依据。 |
| 发布质量 | 性能/平台基础校验已通过 | S8 真机性能、横屏安全区、触摸热区、键盘/手柄焦点、存档损坏恢复与长时间运行验证。 |

### 2.3 当前阶段结论

项目已经越过“框架工程”阶段，S1–S6 构成完整可玩链：建造 → 生存 → 采集/生产 → 探索 → 招募/编队 → 教学 Boss 战均可从正常 UI 推进并保存。S7-01～S7-04 已将竹筏成长、设施数量、区域 Boss、商人交易与新手目标/导航收敛到数据驱动规则，下一步完成 20–30 分钟试玩平衡，再进入发布质量收口。

---

## 3. 已确认的玩法口径

| 编号 | 口径 | 当前处理 |
| --- | --- | --- |
| D01 | 生存为“补给、耐久”两个生存指标，体力为独立行动资源 | 已按 `SurvivalConfigDefinition` 和 `SurvivalState` 落地。旧原型中的士气不进入 MVP。 |
| D02 | 设施只生产物品；补给由玩家手动使用食物/淡水恢复 | 已落地：集雨器产淡水、篝火产烤鱼，二者通过 UseFoodCommand 恢复补给。 |
| D03 | 首批容量由 Definition 配置，S7 再平衡 | 已落地：淡水/烤鱼基础容量为 20/10，储水罐使淡水容量提升至 50。 |
| D04 | 环境修正采用加法叠加并受上限约束 | 计算器接口和配置已预留；天气/海域来源在 S4 后分阶段接入。 |
| D05 | 战斗体力在进场前一次性预扣，回合内不重复扣除 | 保留为 S6 规则，S2 收口时先提供通用预检/扣除接口。 |
| D06 | 归零不致死；低值和归零必须有可理解反馈 | HUD 已显示警告/归零状态；离线报告和行动失败反馈待补齐。 |
| D07 | 离线补给最低保留 1；体力恢复由配置控制；离线收益另由生产/离线系统处理 | 公式已存在，实际读档结算与报告列为当前 P0。 |

策划表中的数据库字段、整数 ID 和全局事件总线不直接照搬。静态数据使用 JSON Definition 与 `StringName` 稳定 ID；运行时数据保存于 State/Instance；跨模块由公开 System 接口、Command 或领域 Signal 交互。

---

## 4. 后续路线总览

```text
已完成：S0 验证基线 → S1 建造 → S2 在线生存 → S3 采集/生产
  → R0 生存与指令收口 → S4 大地图与确定性探索
  → S5 伙伴卡牌与编队 → S6 教学 Boss 战
                                      ↓
当前：S7 MVP 整合、引导与平衡（S7-01～S7-04 已落地，S7-05 平衡进行中）
                                      ↓
S8 发布质量与内容扩展（持续）
```

工作量仅用于控制任务粒度：S（小）、M（中）、L（大）、XL（跨模块大型任务），不代表日历工期。

---

## 5. 已完成优先事项：R0 生存与指令收口（P0，工作量 M）

**目标（已于 2026-08-05 落地并通过 `-Full` 验证）**：让 S4/S6 能以稳定的公开接口消耗体力、造成耐久影响、在读档时完成幂等的离线生存结算；同时保持 UI 不直接修改 State。

### R0-01：补齐 SurvivalSystem 的行动规则接口

- 已落地：`can_perform_action` / `consume_action_stamina` / `apply_durability_loss` 公开接口，返回值含成功、失败码与消耗量；Exploration/Battle 不直接写 `SurvivalState`。
- 已落地：行动类型与失败码使用 `StringName`，探索与战斗复用同一套预检规则。
- 已落地：补给归零不阻塞行动、耐久归零禁止战斗、体力不足拒绝并发出 `survival_action_rejected`。
- 已落地：不足体力、边界值、一次性扣除、耐久归零等单元测试。

### R0-02：实现离线生存结算与存档幂等性

- 已落地：`GameSession.load_state_at` 激活入口调用 `SurvivalCalculator.calculate_offline()`，更新状态、时间戳与幂等标记。
- 已落地：离线补给最低保留 1、耐久不离线损耗、体力按配置恢复；重复载入不重复结算。
- 已落地：只读 `OfflineSettlementReport` 供 UI 展示，不保存 Node。
- 已落地：0/1/4/8 小时、超长离线、时钟倒退、重复结算、迁移测试；`OfflineSettlementPanel` 已接入 GameRoot 读档流程。

### R0-03：统一 Command 的进入路径

- 已落地：Gather、UseFood、SetProductionEnabled 均已注册 `SessionCommandSystem` 分支，成功/失败与 `command_rejected` 事件一致。
- 已落地：View 只创建 Command，GameSession 是 State/System 拥有者，未新增全局 Command Manager。
- 已落地：建造、采集、食用、设施启停的成功/失败/拒绝事件测试。

**R0 验收**：探索系统无需访问 `SurvivalState` 内部容器即可完成行动预检与扣除；离线结算可重入且可保存；完整测试和 `git diff --check` 通过；桌面和触摸下的现有生存/生产操作未回归。

## 5.1 当前优先事项：S7-01 竹筏成长与数据驱动解锁（P0，工作量 S，本次已落地）

**目标**：让竹筏 Lv1→Lv3、3×3→5×6 扩建和船舵解锁进入数据驱动规则，UI 不再自行判断解锁条件。

- 已落地：`ProgressionDefinition`（raft_level / unlock 两种节点）与 `Data/Progression/` 配置；`ProgressionState` 保存 `raft_level` 并序列化。
- 已落地：`ProgressionSystem` 负责扩建校验、库存原子扣费、甲板扩张与 `raft_upgraded` Signal；失败不产生部分状态修改。
- 已落地：船舵解锁改为查询 `unlock_exploration` 数据规则（要求 `building_rudder`），`ExplorationSystem` 与 UI 均通过 `GameSession.is_unlock_available()` 读取。
- 已落地：`UpgradeRaftCommand` 进入 `SessionCommandSystem` 统一派发；设施面板新增竹筏等级与扩建按钮。
- 已落地：存档升级至 v9，`_v8_to_v9` 为旧档补充 `progression_state.raft_level`；单元测试覆盖扩建、失败原子性、解锁、存档往返与迁移。

**S7-01 验收**：竹筏可从 3×3 升到 5×6；`verify_project.ps1 -Full` 通过（79 项测试）；旧档（v8）可迁移；桌面与触摸现有操作未回归。

## 5.2 当前优先事项：S7-02 设施扩充与区域 Boss（P0，工作量 M，本次已落地）

**目标**：将设施数量推进到 7–10 个，并补齐教学 Boss 之外的区域 Boss，让中期成长有更多选择。

- 已落地：新增 `building_desalinator`（海水蒸馏）、`building_fishing_net`（自动捕鱼）、`building_storage_rack`（扩容储物），设施总数 5→8。
- 已落地：新增 `recipe_desalinate`（2 海水 → 1 淡水）与 `recipe_fish_net`（无需输入，周期性产出生鱼），均经 `ProductionSystem` 批量结算并受库存容量约束。
- 已落地：新增 `boss_reef_leviathan`（暗礁巨鳗）与 `reward_reef_cache`；`BossDefinition` 增加可选 `unlock_id`，区域 Boss 经 `unlock_boss_reef` 数据规则（竹筏 Lv2）解锁，`GameSession.execute_start_battle` 强制校验。
- 已落地：建造面板、生产面板与伙伴面板均接入新内容入口；Boss 解锁状态由 `GameSession.is_boss_unlocked()` 提供，UI 不自行判断。
- 已落地：数据注册校验覆盖 Boss→解锁规则引用；单元测试覆盖淡化/渔网/扩容、区域 Boss 解锁门禁与战斗奖励/耐久结算。

**S7-02 验收**：新设施可从 UI 建造并正常生产；区域 Boss 在竹筏 Lv2 前无法挑战且不扣体力；`verify_project.ps1 -Full` 通过（84 项测试）。

## 5.3 当前优先事项：S7-03 流浪商人模板与原子交易（P0，工作量 S，本次已落地）

**目标**：提供一个数据驱动的商人模板，让玩家用海货换取急需物品，交易全程原子且可存档。

- 已落地：`MerchantDefinition`/`MerchantOfferDefinition` 与 `Data/Merchants/wandering.json`（淡水、烤鱼、木材捆三个条目，含价格与初始库存）。
- 已落地：`MerchantState` 序列化库存并随存档往返，存档升级至 v10（`_v9_to_v10` 为旧档补充空库存）。
- 已落地：`MerchantSystem` 在购买前校验条目、库存、成本与容量，再一次性扣费、扣库存、发放物品；失败不产生部分修改。
- 已落地：`BuyMerchantItemCommand` 接入 `SessionCommandSystem`；商人面板与底部导航“商人”按钮已接入，按钮按公开查询禁用，UI 不直接修改 State。
- 已落地：数据注册校验覆盖商人→物品/成本引用；单元测试覆盖初始化库存、原子购买、售罄/容量拒绝、多件条目、存档往返与 v9 迁移。

**S7-03 验收**：商人可从 UI 交易；失败无部分扣费；存档重载后库存一致；`verify_project.ps1 -Full` 通过（91 项测试）。

## 5.4 当前优先事项：S7-04 新手目标与统一导航（P0，工作量 S，本次已落地）

**目标**：用数据驱动的新手目标把“探索→收集→扩建→招募→战斗”串成可跟随的路径，并让玩家一键跳转到对应面板。

- 已落地：`QuestDefinition`（build_building / have_item / upgrade_raft / recruit_survivor / win_battle / explore_region 六种目标）与 `Data/Quests/` 五条新手目标链（集雨器 → 驶向西部浅滩 → 救起伙伴 → 竹筏 Lv2 → 教学 Boss）。
- 已落地：`QuestSystem` 从 GameState 派生进度，达成时写入 `QuestState.completed_quest_ids`；Boss 胜利经 `defeated_boss_ids` 持久化，防止读档后目标回退。
- 已落地：存档升级至 v11（`_v10_to_v11` 为旧档补充空目标状态）；完成标记与击败 Boss 完整往返。
- 已落地：底部导航新增“目标”按钮（替换与“补给”重复的“仓库”按钮），目标面板展示当前进度并带“去完成”按钮，按目标类型一键跳转建造/补给/伙伴/航海图面板。
- 已落地：数据注册校验覆盖目标→建筑/物品/伙伴/Boss/海域引用；单元测试覆盖五目标链、进度派生、完成持久化、存档往返与 v10 迁移。

**S7-04 验收**：新档可从目标面板按顺序跟随五步新手链；跳转面板正确；击败 Boss 后读档目标不回退；`verify_project.ps1 -Full` 通过（96 项测试）。

---

## 6. MVP 分阶段开发计划

### S4：大地图与确定性探索闭环（P0，工作量 XL）——已落地

**玩家可感知结果**：建造并解锁船舵后，玩家可在起始海域与 2–3 个相邻六边形海域间航行；预览成本并确认行动；获得确定性资源、空事件或风暴结果，再回到竹筏循环。

**实施顺序**：

1. `RegionDefinition`、`EncounterDefinition`、最小 `WorldState`：当前位置、已发现区域、已消费一次性事件与世界随机状态；所有引用使用 `StringName`。
2. 纯规则 `ExplorationSystem`：相邻性、发现、解锁条件、行动成本、原子扣除和结果对象；先接入 R0 的体力/耐久接口。
3. `RandomService` 独立 exploration 流：相同 world seed 与相同状态必得相同遭遇，不污染未来 battle 流。
4. 四种最小遭遇：资源发现、空事件、风暴、救援；奖励经 InventorySystem 发放，风暴经 SurvivalSystem 处理耐久影响。
5. 地图 View：可达性、成本预览、确认/取消、遭遇结果及返回竹筏。View 只读状态和结果。
6. 存档：当前位置、已发现状态、一次性事件和必要随机状态完整往返。

**暂不包含**：完整天气系统、昼夜、真实海域温度、交易和 Boss。天气作为后续独立模块，S4 仅预留环境修正输入接口，不用临时硬编码天气规则。

**测试重点**：不可跨格、资源不足不出发、失败无部分扣费、同 Seed 同遭遇、一次性事件不重复、风暴耐久损耗、读档后世界状态一致。

### S5：伙伴卡牌与编队（P1，工作量 L）——已落地

**目标**：由探索救援获得 3–4 名伙伴，支持详情、Lv1–Lv10 升级和 2–3 个出战位。

- 定义 Survivor/Skill Definition、SurvivorInstance、SurvivorState 和稳定 ID；同一唯一伙伴不得重复招募。
- 实现 Recruit、Upgrade、SetLineup Commands 及 SurvivorSystem，所有消耗经 InventorySystem 原子处理。
- 先提供最小被动效果查询接口；组合加成、品质突破和完整故事不进入 MVP。
- 接入救援遭遇、卡牌/编队 UI、存档往返及触摸友好替换操作。

### S6：教学 Boss 战（P0，工作量 XL）——主体已落地

**目标**：当前编队可挑战一个教学 Boss，使用普通攻击和至少一个技能，获得胜负、掉落和返回主游戏的完整结果。

- 定义 Boss/Skill/Reward Definition 与 BattleState；BattleSystem 为纯规则，不依赖动画帧或具体场景节点。
- 进场通过 R0 的接口预扣一次体力并校验耐久；战斗回合内不再扣体力。
- 使用独立 battle 随机流；结果通过明确接口结算掉落、伙伴经验和耐久影响，避免直接操作其他模块内部容器。
- 先实现教学 Boss；区域 Boss 作为 S7 内容补齐。BattleRoot 只表现状态变化与行动意图。

### S7：MVP 整合、引导与平衡（P0，工作量 XL）——进行中

**目标**：新玩家能在约 20 分钟内自然完成一次“探索→收集→扩建→招募→战斗→成长”。

- [x] S7-01：竹筏 Lv1→Lv3、3×3→5×6 扩建、船舵解锁及 `ProgressionSystem`；UI 不自行判断解锁条件（本次完成）。
- [x] S7-02：设施扩充至 7–10 个、补齐区域 Boss（当前 8 个设施、教学 + 区域 Boss，本次完成）。
- [x] S7-03：增加一个数据驱动的流浪商人模板，交易经 `InventorySystem` 原子交换（本次完成）。
- [x] S7-04：建立数据驱动的新手目标与导航；完成建造、背包、地图、伙伴、战斗、离线面板之间的返回路径（本次完成）。
- [ ] S7-05：完成 20–30 分钟试玩，平衡补给消耗、恢复、产能、航行、奖励与战斗；记录调参依据。

海域（4 个已规划 + 动态远洋）与伙伴（4 名）数量已达到 MVP 目标，后续仅按试玩结果微调。

### S8：发布质量与 MVP 后内容（P1/P2，持续）

先做真机性能、横屏/安全区、触摸热区、键盘/手柄焦点、设置、音效、基础可访问性、存档损坏/备份恢复和长时间运行验证。之后再按试玩结果扩展天气、更多海域/遭遇/设施/伙伴/Boss、任务、据点与叙事。

不进入当前 MVP：完整 10+ 海域地图、复杂程序化世界、卡牌组合加成、品质突破、完整剧情/据点、抽卡、广告/付费、社交/排行榜、账号/云同步/联机、ECS、C#、SQLite 和大型第三方框架。

---

## 7. MVP 需求追踪

| 策划 MVP 项 | 当前状态 | 主要后续阶段 |
| --- | --- | --- |
| 竹筏 Lv1→Lv3、3×3→5×6 | S7-01 已完成（数据驱动扩建） | 已落地 |
| 7–10 个设施 | 已有 8 个 | 已落地 |
| 3–4 个海域 | 4 个已规划海域 + 动态远洋 | 已落地 |
| 基础/加工/生存资源 | 首条资源与生产链已完成 | S7 按试玩扩充 |
| 3–4 张伙伴卡、Lv1–Lv10 | 4 名伙伴、升级与编队完成 | 已落地 |
| 2–3 个出战位 | 2 个出战位已实现 | 已落地 |
| 教学 Boss + 区域 Boss | 两个 Boss 均已落地 | 已落地 |
| 流浪商人 | 1 个商人模板、3 个交易条目 | 已落地 |
| 完整核心循环 | 建造/生存/探索/伙伴/战斗可操作，新手目标与导航已接通 | S7-05 平衡收口 |

---

## 8. 每阶段通用完成定义

一个阶段只有同时满足以下条件才可标记完成：

1. 数据归属、System 修改权、Command/Signal 流向清楚，UI 不直接写 State；
2. 可调数值进入 Definition/配置文件，稳定引用使用 `StringName`；
3. 新增 State/Instance 已序列化，旧档有迁移测试或明确拒绝策略；
4. 规则有单元测试，跨模块链路有集成验证，随机逻辑使用固定 Seed；
5. `git diff --check` 与 `Tools/verify_project.ps1 -GodotPath <Godot可执行文件> -Full` 通过；
6. 正常、失败、返回流程可操作，错误反馈可理解；
7. 桌面与触摸至少各验证一次关键流程，并检查横屏安全区；
8. 逻辑不绑定渲染帧，离线与长时间结算用公式或批量周期计算；
9. 不混入未授权 Autoload、第三方框架、联网、账号、云同步或无关重构。

---

## 9. 推荐的下一步开发任务

当前基线已越过 R0/S4/S5/S6，进入 **S7 MVP 整合**。按可独立验收的行为拆分，建议顺序如下：

```text
S7-01 竹筏成长与数据驱动解锁（已落地）
  → S7-02 设施扩充与区域 Boss（已落地）
  → S7-03 流浪商人模板与原子交易（已落地）
  → S7-04 新手目标、导航与返回路径（已落地）
  → S7-05 20–30 分钟试玩与平衡调参
  → S8 发布质量与 MVP 后内容
```

每个后续开发任务应限制在一个可独立验收的行为内，并注明：允许修改目录、输入 Definition/State、公开接口、失败与边界行为、存档影响、测试命令以及明确不包含项。

---

*——《沧海余生》后续开发计划 v0.2——*
